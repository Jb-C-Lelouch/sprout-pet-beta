# 本地数据说明

本版本花园代码不包含向游戏服务器上传数据的功能；安装和更新插件仍会通过 Codex/Git 访问仓库，Codex 自身的数据处理不属于本插件的范围。

插件钩子读取宿主提供的本地 transcript 文件，逐行解析以提取会话标识和累计 token 数。因此它会接触包含对话内容的记录；本插件不把对话正文复制进花园存档，也不将正文上传。

用户主目录 `.codex-pet` 保存宠物成长、会话标识与计数基线、精力、植物、收藏、收获、界面设置和连接状态。自定义头像设置可能保存本地图片路径。可通过 CODEX_PET_DATA 或程序 --data 参数使用其他存档目录。

自然成长不要求 token 授权。禁用插件钩子后，花园可继续按时间成长，但不能从 Codex 新用量补充精力。卸载插件默认不会删除存档；删除存档会丢失进度，应先备份。

环境检查的 --smoke 选项仅使用临时存档和合成用量，结束后清理，不修改个人进度。
